Tommy_branch:
to get the data generated run this command and make sure you have a java JDK version greater than 11:

'java -jar synthea-with-dependencies.jar -p 1000 -s 12345 --exporter.csv.export=true --exporter.fhir.export=false Texas'

as of right now I think all the package you need for my branch is pandas

run:
'python main.py'
you'll get a list of all columns
